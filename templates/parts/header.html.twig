<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="{{ path('default_location') }}">
            Deloitte
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('default_location') }}" class="nav-link" role="button">{{ 'navbar.home' | trans }}</a>
                </li>
                {% if 'ROLE_ADMIN' in app.user.roles %}
                    <li class="nav-item">
                        <a href="{{ path('users') }}" class="nav-link" role="button">{{ 'navbar.employees.list' | trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('user_add') }}" class="nav-link" role="button">{{ 'navbar.employees.add' | trans }}</a>
                    </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link">{{ 'navbar.logout' | trans }}</a></li>
            </ul>
        </div>
    </nav>
</header>
